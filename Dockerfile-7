FROM centos:7
MAINTAINER tjinjin

# setup
RUN yum update -y
RUN yum install -y rpm-build tar make

# ruby depends
RUN yum -y install readline-devel ncurses-devel gdbm-devel glibc-devel gcc openssl-devel libyaml-devel libffi-devel zlib-devel

# rpmbuild command recommends to use `builder:builder` as user:group.
RUN useradd -u 1000 builder

# build
RUN mkdir -p /home/builder/rpmbuild/{BUILD,BUILDROOT,RPMS,SOURCES,SPECS,SRPMS}
RUN chown builder:builder /home/builder/rpmbuild/{BUILD,BUILDROOT,RPMS,SOURCES,SPECS,SRPMS}
WORKDIR /home/builder/rpmbuild
ADD script/rubybuild.sh /home/builder/rpmbuild/rubybuild.sh
ADD ruby.spec /home/builder/rpmbuild/SPECS/ruby.spec
ADD ruby-version /home/builder/rpmbuild/SOURCES/ruby-version
